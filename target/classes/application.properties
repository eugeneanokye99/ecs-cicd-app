server.port=8080

# Actuator health endpoint — used by ALB health checks indirectly
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

spring.application.name=ecs-cicd
